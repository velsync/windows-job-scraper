# Production runtime dependencies (cross-platform wheels).
# Locked exact versions live in requirements/production.lock.txt.
# NOTE: pywin32 is a Windows-only runtime dependency; it is declared here and
# resolved on Windows installs (see packaging/). It cannot be locked on the
# cross-platform dev environment.
fastapi>=0.115
uvicorn>=0.30
jinja2>=3.1
tzdata>=2024.1
playwright>=1.45
