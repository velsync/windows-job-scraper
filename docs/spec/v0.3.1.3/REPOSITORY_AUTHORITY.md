# Repository Authority Note

The files in this directory are the canonical repository copy of Windows Job Scraper v0.3.1.3. The eight numbered Markdown modules are normative according to `00_architecture_overview_and_authority.md`; `CORRECTIONS_v0313.md` is non-normative history; `MANIFEST.json` and `CHECKSUMS.sha256` verify the bundle metadata.

Implementation workers MUST NOT edit these files to resolve implementation difficulties. A genuine contradiction or blocker is escalated to the architecture reviewer and, if accepted, produces an explicit later corrective specification revision.
